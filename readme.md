Bibnet
=======================

## Dependencies
- npm
- gulp => `npm install -g gulp`
- bower => `npm install -g bower`

## Usage
- `npm install`
- `bower install`
- `gulp`
- Server running on `http://localhost:8080`